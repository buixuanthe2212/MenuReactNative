// @flow
// import * as ActionType from './actionTypes';

export default (
  state: Object = {
    isLogIn: false,
  },
  action: Object => Object,
) => {
  switch (action.type) {
    default:
      return state;
  }
};
