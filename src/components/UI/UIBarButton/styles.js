// @flow
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 60,
    height: 46,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 8,
  },
  icon: {
    width: 20,
    height: 20,
    resizeMode: 'contain',
  },
});
